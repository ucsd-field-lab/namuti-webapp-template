from time import time
import logging


current_user = None
logged_in = current_user['name'] + " has logged in at " + time
updated_db = current_user['name'] + " updated the database at " + time


def log():
	return




